<%= form_for @order do |f| %>

    <%= f.label "Description of order: " %><br>
    e.g. Carla's bridal and groom's cake
    <br><%= f.text_area :description%><br><br>
    <%= f.hidden_field (:customer_id) %><br>
    Cakes in order:
    
    <%= f.fields_for :cakes do |cake| %>
      
        <br><%= cake.label :title %><br>
        <br><%= cake.text_field :title %><br>
    
        <%= cake.label :number_tiers %>
        <%= cake.number_field :number_tiers, in: 1...10 %>
   
    <% end %>
    
  
    <%= button_to "Create order", orders_path, method: :post %> 
    <% end %>

    <%= render 'shared/links' %>
